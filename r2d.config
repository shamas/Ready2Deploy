#these must be the filenames only in the same directory as the r2d executable
inputfile='r2d.files'
tforms='r2d.transforms'
#rootdirectory of the project
rootdir='./'
#the output directory relative to the $rootdir
out='live/'
